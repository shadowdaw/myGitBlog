extends layout

block content
	.header
		div.flex
			div.logo 
			a(href="/")
				span 失眠宇宙's Blog
		div.indexbtn
			a(href="/")
				span 首页
	.container
		.markdown-body
			h2 #{blog.title}
			p.tips 
				span #{blog.time}
				a
					span #{blog.category}
			!= mdContent 
	p.end END
	.footer
		span 我记录的都是“那时候的我真傻逼”的证据.
		div.imgarea
			img.footerimg(src='http://static.insomnia-er.com/image/pay2comment.jpg')
		span 付款码无姓名,如需要解答问题请留下联系方式.
	script.
		var ua = navigator.userAgent.toLowerCase();
		if (ua.match(/MicroMessenger/i) == "micromessenger") { 
		}
		var scrolltop=0;
		window.onscroll = function(e){
			var tempscrolltop=document.documentElement.scrollTop||document.body.scrollTop;
			if(tempscrolltop>scrolltop && tempscrolltop>60){
				document.body.className='scroll'
			}else{
				document.body.className=''
			}
			scrolltop=tempscrolltop;
		} 
		window.onload = function() {
			getWechatConifg();
			prepareShare({
				title: '#{blog.title}',
				summary: '#{blog.title}',
				shareUrl: window.location.href,
				imgUrl: '#{blog.imageUrl}'
			})
		};
   
