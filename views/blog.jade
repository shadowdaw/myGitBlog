extends layout

block content
	.header
		div.flex
			div.logo 
			a(href="/")
				span 失眠宇宙's Blog
	.container
		.markdown-body
			h2 #{blog.title}
			p.tips 
				span #{blog.time}
				a
					span #{blog.category}
			!= mdContent 
	p.end END
	.footer
		span 我记录的都是“那时候的我真傻逼”的证据.
		div.imgarea
			img.footerimg(src='http://owyx09dkb.bkt.clouddn.com/image/pay2comment.jpg')
	script.
		var ua = navigator.userAgent.toLowerCase();
	    if (ua.match(/MicroMessenger/i) == "micromessenger") { 
	    } else {
	      document.getElementsByClassName('footer')[0].style.display='none';
	      document.getElementsByClassName('end')[0].style.display='none';
	    }
		var scrolltop=0;
		window.onscroll = function(e){
			var tempscrolltop=document.documentElement.scrollTop||document.body.scrollTop;
			if(tempscrolltop>scrolltop && tempscrolltop>60){
				document.body.className='scroll'
			}else{
				document.body.className=''
			}
			scrolltop=tempscrolltop;
		}  
   
